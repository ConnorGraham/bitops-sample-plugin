my-fav-color:
  type: object
  properties:
    options:
      type: object
      properties:
        name:
          type: string
          export_env: MY_NAME
          default: world
        color:
          type: string
          export_env: MY_FAV_COLOR
          default: blue
